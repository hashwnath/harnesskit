# Example: tokio-rs/axum (Rust)

Generated by running:

```bash
git clone https://github.com/tokio-rs/axum
cd axum
npx harnesskit init --yes
```

**Auto-detected:** `rust` project (via `Cargo.toml`), `vscode` IDE, `github` git provider

## Files in this directory

These are the key files generated by `harnesskit init`. The full scaffold includes additional files — see [EXAMPLES.md](../EXAMPLES.md) for the complete file tree.

| File | Purpose |
|------|---------|
| `AGENTS.md` | Universal agent entry point |
| `ARCHITECTURE.md` | Layer rules and dependency graph |
| `QUALITY_SCORE.md` | Per-domain quality grades |
| `SECURITY.md` | Security posture and data classification |
| `RELIABILITY.md` | Bootability, health checks, SLAs |
| `core-beliefs.md` | 8 agent-first operating principles |
| `planner.agent.md` | Planner agent (read-only, creates plans) |
| `implementer.agent.md` | Implementer agent (writes code) |
